<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_sign_in"
            android:icon="@drawable/ic_user"
            android:title="Sign in" />
        <item
            android:id="@+id/nav_split_utilities"
            android:icon="@drawable/ic_bill"
            android:title="Split Utilities" />
        <item
            android:id="@+id/nav_split_household"
            android:icon="@drawable/ic_clean"
            android:title="Split household supplies" />
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings"
            android:title="Settings" />
    </group>

    <item android:title="Manage">
        <menu>
            <item
                android:id="@+id/nav_apartment"
                android:icon="@drawable/ic_add"
                android:title="Create Apartment" />
            <item
                android:id="@+id/nav_add_people"
                android:icon="@drawable/ic_add"
                android:title="Add people" />
        </menu>
    </item>

</menu>
